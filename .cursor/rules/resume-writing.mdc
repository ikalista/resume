---
description: 简历编写与项目材料维护规范
alwaysApply: true
---

# 简历维护规范

## 规则冲突处理

- 如果认为本规则内部、规则与当前简历任务要求或不同事实源之间存在冲突，禁止自行取舍或折中；必须先向用户说明具体冲突并询问处理方式，确认后再继续，同时询问是否需要据此调整本规则。

## 事实源与真实性边界

- 每个岗位方向在 `岗位简历/<岗位方向>/` 下使用独立文件夹；其中 `<岗位方向>-李嘉辉.md` 是用于投递的岗位简历。
- 项目目录下的 `简历.md` 是项目级最终事实源，`面试稿.md` 是项目级口述事实源；岗位简历直接从项目材料装配。
- 仓库顶层 `管理经历/` 是独立演进的管理主题材料，不归属任何岗位，也不要求与岗位材料同步；不得再套设“通用材料”等中间目录。
- 岗位简历与项目级 `简历.md` 必须同步；项目事实、指标或表述变化必须先落到项目级 `简历.md`，再更新岗位简历，禁止只改任一侧。
- 信息优先级：项目 `简历.md` > `关键节点.md`/`相关材料/` > `流水账.md`；`流水账.md` 是原始素材，除用户明确要求外不要大改。
- 发现时间、指标、项目名冲突时，采用更具体、可验证、来源更近的事实，并同步消除冲突。
- 真实参与边界必须明确：不是本人原创/主导的内容，简历可写复现、对齐、落地、工程衔接，但不能写成独立主导原创；面试稿要直说真实角色。

## 工作流

1. 先读目标岗位简历，判断岗位侧重、项目排序和篇幅。
2. 再读相关项目的 `简历.md`；信息不足再读 `面试稿.md`、`关键节点.md`、`相关材料/`、`流水账.md`。
3. 项目事实、指标或表述变化必须先修改项目级 `简历.md`；涉及真实参与方式、面试解释或容易被追问的边界时，必须新增或更新项目级 `面试稿.md`。
4. 再从项目级 `简历.md` 抽取稳定内容，更新岗位简历，最后检查事实一致性、格式和高风险措辞。

## 投递文本通用规范

- 默认按近 5 年 NLP、音视频与 AI 算法经验行文，并根据目标岗位采用资深算法工程师、Tech Leader 或算法架构师视角；机器人 / 具身智能属于转行方向，突出可迁移的算法与工程能力及个人项目闭环，不宣称机器人行业从业年限。
- 用正式书面语，避免聊天式表达。
- 禁止“学习了”“尝试了”“熟悉了”等成长型表述，改为具体产出。
- 避免“协助”“配合”“参与了解”等虚化措辞；参与型项目只写核心贡献。
- 行动要点主语省略，直接用动词开头。
- 结果必须量化，并说明变化前后或对照基线。
- 小样本指标必须保守表达，例如“10 次验证中达到 80%+ 成功率”，不要泛化成“准确率 80%+”。
- 禁止“显著提升”“大幅改善”“效果很好”等模糊结果。
- 禁止口语、自我辩解和未证实夸张，如“阴差阳错”“我不在意薪酬”“录用我要冒风险”“行业领先”“最优路径”“完全解决”“100%准确率”。

## 岗位简历规范

### 文件与格式

- 每个岗位方向在 `岗位简历/` 下使用同名文件夹，岗位简历命名为 `<岗位方向>-李嘉辉.md`；岗位材料不得散放在仓库根目录或 `岗位简历/` 根目录。
- 岗位简历标题下方第一组 `::: left` / `::: right` 联系方式区块是简历自动生成器专用协议。除非用户明确要求，禁止新增、删除、改写其中任何内容，也禁止调整其中的空行、注释、链接格式、图标标记和占位内容。
- 正文优化从教育背景及后续内容开始；教育背景中的 `:::left` / `:::right` 可优化，但必须保持生成器可解析。

### 结构与篇幅

- 默认顺序为：标题和联系方式、教育背景、个人简介、工作经历、项目经历、其他项目。
- 个人简介默认 3 条，采用“系统规模 / 架构职责 / 结果指标”三类信息；其中最多 1 条可包含 3-6 个与目标岗位强相关的模型或框架，用于建立技术锚点。
- 默认不单设“核心能力”“多模态能力”“技术能力”等与个人简介、项目经历重复的能力清单。
- 仅当内容无法被个人简介和项目经历替代时才增加特殊模块；具身智能个人项目可以作为独立经历板块，平行于工作经历，不视为能力清单。
- 常规岗位简历最多展开 5 个核心项目；其余项目精选 3-5 项，以每项 1 句话的形式收敛到末尾“其他项目”。具身智能岗位简历不受核心项目数量限制。
- “数据智能与建模智能（Text2SQL）”必须在所有岗位简历中作为核心项目展开，不得降级到“其他项目”。
- 篇幅优先控制在 2 页左右；核心项目证据无法无损压缩时允许 3 页，禁止为了页数删除关键指标、职责边界或强结果。

### 岗位侧重

- 具身智能 / VLA：优先 VLA、机器人、真机采集、仿真复现、多模态、强化学习；公司项目作为工程闭环补充。
- 多模态大模型：优先 VLA、多模态反欺诈、QwenVL/Qwen-Omni、视觉/声学/文本融合。
- NLP / 大模型：优先 RAG、Agent、坐席智能、Text2SQL、SFT/LoRA、推理部署和评测体系。
- AI 基建：优先 Docker、K8s、vLLM、LiteLLM、AIBrix、GPU 调度、开发平台和推理服务治理。
- 强化学习 / 决策优化：优先 VLA 策略学习、LIBERO 复现、动作生成、仿真到真实迁移；资金资产匹配可作为约束优化补充。

## 项目级材料规范

- 项目级 `简历.md` 默认采用 `背景 + 行动`：背景 1 句话；行动 3-5 条，写清职责、关键决策、方案、实现和结果。技术栈与指标写进行动要点，不单独堆关键词。
- 项目级 `面试稿.md` 记录真实口述口径：分别说清原创、复现、跟做、落地、工程衔接和指标复盘；说明自己做了什么、别人做了什么、最终结果及被追问时的边界；可记录不适合写入简历的动机、取舍、失败和学习曲线。

## 岗位面试材料规范

- 岗位文件夹可按需创建 `技术面试稿.md`、`HR面试稿.md` 和 `HR底稿.md`，不强制补齐空文件。
- `技术面试稿.md` 与 `HR面试稿.md` 是可对外口述的面试材料，可以比投递简历更直白、更个人化，保留真实动机和原味表达。
- 面试稿允许提到行业压力、当前薪资、愿意为转方向降薪，以及“高风险高回报候选人”的自我定位；这类表达必须服务于面试策略，说明转岗决心、学习速度、全链路闭环能力和入职后回报空间，不能写成纯抱怨或自我贬损。
- `HR底稿.md` 用于记录内部事实、薪资底线、风险判断和表达禁区，不得直接对外展示或逐字复述。

## 交付检查

- 联系方式生成器区块未被改动。
- 项目经历类改动已经先落到对应项目级 `简历.md`，不存在只改岗位简历的情况。
- 真实参与边界已经写入对应项目级 `面试稿.md`，不存在简历说法和面试说法冲突。
- 岗位简历和项目级 `简历.md` 的时间、指标、项目名称一致。
- 无冲突指标、高风险口语、自我辩解、模糊结果和夸张结论。
- Markdown 标题层级、项目编号和项目排序符合目标岗位；如有 git 环境，运行 `git diff --check`。
