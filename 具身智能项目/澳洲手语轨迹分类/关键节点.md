- 认为关键点是一种人工设计的高级特征，确定使用关键点进行动作识别
- 进行research，知晓关键点识别的关键工作，openpose，hrnet，mediapipe
- 确定基于关键点进行动作分类的工作，选取 ST-GCN 作为复现目标
- 探明ST-GCN有较高的适配性，2D关键点，可以输入x,y,置信度，3D关键点可以输入x,y,z
- 探明 ST-GCN 常见数据集为 Kinetics 系列与 NTU-RGB+D 60；知悉 Kinetics 数据量远大于 NTU（约 7–8 倍），实验仅在 NTU-RGB+D 60 子集上复现，未在 Kinetics 上训练
- 基于我们的显卡（2060 Super 8G显存），选用NTU-RGB+D 60的部分数据集进行跑通（日常行为 (Daily Actions)：共 40 类 (A1-A40)），复现结果指标xx%
- 在手语数字上开始复现，使用mediapipe提取2D关键点，测试集指标97%

    | 版本 | val acc | test acc | 问题 |
    |---|---|---|---|
    | 初版（灰度，475样本） | 42.7% | 34.0% | 样本少 + 无归一化 |
    | 彩图+temporal=9 | 30.6% | 28.8% | 无归一化，模型崩塌预测class 8 |
    | +temporal=1 | 25.6% | 23.3% | 同上，clip bug |
    | +归一化+balanced+fix | 97.5% | 97.0% | ✅ |
结果分析：Test 96.97%，10 类中有 6 类 100%，最差的 class 6 是 85.2%（混淆到 2/5/9）。

- 在手语字母表上开始复现，使用mediapipe提取2D关键点，指标xx%
- 在手语常用于上尝试复现，受到平均脸算法的启发，使用mediapipe提取3D关键点，但是发现深度没法绝对建模。并且由于3D关键点在多人身，骨架长度不一致等问题，尝试进行归一化。尝试了先验骨架长度，引导动力学模型。平均骨架关键点，等方案。发现都不能很好的获得3D平均关键点。实验失败。
 