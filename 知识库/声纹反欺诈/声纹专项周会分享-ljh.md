声纹专项分享-

# 分治
分治（Divide and Conquer）是一种算法设计思想，将复杂问题分解为若干个规模较小的相同问题，递归求解这些子问题，然后将子问题的解合并得到原问题的解。

## 分治的核心思想
1. **分解（Divide）**：将原问题分解为若干个规模较小、相互独立、与原问题形式相同的子问题
2. **解决（Conquer）**：若子问题规模较小且易于解决，则直接求解；否则递归求解各子问题
3. **合并（Combine）**：将各子问题的解合并为原问题的解

# 模型并行
数据并行（Data Parallelism）：一个模型一台机器上放得下，为了不浪费机器，多放几个模型。类似于k8s加节点，部署x个模型负载均衡，推理速度就翻x倍（近似）。
模型并行（Model Parallesim）：一个模型单台机器/显卡上放不下：假设模型100层，那就分两个50层，放在两台机器/显卡上。
流水线并行（Pipeline Parallelism）：模型并行后，一个节点运算，另一个节点在等待，计算资源浪费。设计流水线并行填补bubble。
张量并行（Tensor Parallesim）：单层模型，一台机器/显卡都放不下了，模型每层的参数都平均划分在多个机器/GPU上。张量并行相较于模型并行的优点在于，我不需要思考每层的大小，进而对模型进行分配。
专家并行（Expert Parallesim）：专门针对moe模型的一种张量并行

# 声纹算法-高度并行推理优化
## 预处理：
最初的资源及水位情况，5台性能各异的cpu机器，最小的为40核，80核，112核，因此确认pod的size为36核cpu，正好可以1，2，3最大化的利用计算资源。
整个链路串行执行，耗时最高的是降噪。
10万/天

### 降噪模型慢（痛点） - 多种并行方案探索
**方案1：批处理优化**
尝试修改源码支持批处理，发现输入音频长度不一致会padding到相同长度，进而导致耗时增加。进一步可用类大模型的技术（in-flight batching）来解决，出于开发时间成本考虑，放弃该方案。

**方案2：GPU + fork多个模型**
modelscope在fork下锁显卡, 无法进行超越显卡数量的并行，放弃该方案。

**方案3：GPU + spawn多个模型**
在A100上运行正常，在A30上受限于显存大小，且受限于显卡数量（我们只有8卡A30），远远不能实现一天100万的推理目标，放弃该方案。

**方案4：CPU + spawn多个模型**
在A100上10并行度提效成功，确认上线cpu应用。

## 预处理优化总览
- 整个流程资源闲置（痛点）- 流水线并行（10万 -> 36万）
- 稳定性优化（痛点）- 优化降噪/下载数据/上传oss，等模块导致的任务进程死亡，进而需要频繁重启（不再需要频繁重启k8s pod）
- 取数慢，和oss交互慢，模型加载慢 - 批次取，多线程io，扩网络带宽，预加载模型（36万 -> 45万）
- 生产上降噪模型慢 （痛点）- 进行实验发现最佳并行度（45万 -> 90万）
- 计算资源不足 - 加机器 - 新增4台A30做cpu用（90万 -> 170万）
- 计算资源不足 - 加机器 - 新增8台112C高性能机器（170万/天 -> 350万/天）


## 声纹向量模型：
baseline
```python
# 耗时10分钟 -- 耗时占比较高，痛点所在，进行优化 -- 单pod 100万/天
for url in df:
   audio = download(url)
   audios.append(audio)

# 耗时2分钟
model(audios)
```

优化后
```python
# 使用多线程取数及落磁盘，耗时2分钟，局部速度提升5倍，embedding操作速度提升3倍 -- 单pod 360万/天
with ThreadPoolExecutor(max_workers=8) as executor:
   audios = executor.map(download, args=(urls,))

# 耗时2分钟
model(audios)
```

## 总结：从声纹推理走向，模型并行/流水线并行/数据并行/张量并行/专家并行
最终方案：预处理/向量分离
预处理：16台机器流水线并行cpu推理
向量：4台A30向量化处理

数据并行：38 (个pod)
模型并行：2（预处理/embedding分离）
张量并行: 无
流水线并行：有

DeepSpeed ZeRO 系列的 3D 并行度：
ZeRO-0：
数据并行：多个模型多个副本

ZeRO-2：
数据并行：多个模型多个副本，但优化器和状态只有一份
只通信模型参数（ZeRO是通信X and W，张量并行是通信Y），优化器状态 (Optimizer States) 和 梯度 (Gradients) 参数分片

ZeRO-3:
数据并行: 多个模型一个副本，全模型参数分片
只通信模型参数

部署两个vllm的qwen-232b：
数据并行：2
张量并行：4
专家并行：可选

# 声纹算法效果
## 声纹模型横向对比分析
CAM，CAM_ZH_EN，ECAPA

### 模型选型对比

#### EER性能对比
| 数据集 | CAM(%) | CAM_ZH_EN(%) | ECAPA(%) |
|--------|------------|------------------|--------------|
| VoxCeleb | 7.38 | **1.16** | 2.29 |
| CN-Celeb | **6.05** | 7.87 | 19.02 |
| 业务数据集 | 16.9 | **15.66** | 19.24 |

#### AUC性能对比
| 数据集 | CAM | CAM_ZH_EN | ECAPA |
|--------|---------|---------------|-----------|
| VoxCeleb | 0.980 | **0.999** | 0.997 |
| CN-Celeb | **0.985** | 0.899 | 0.887 |
| 业务数据集 | 0.894 | **0.899** | 0.879 |

#### CAM模型降噪前后声纹识别性能对比
| 数据集 | 降噪前EER(%) | 降噪后EER(%) |
|--------|-------------|-------------|
| 业务数据集 | 27.5 | **16.9** |

| 数据集 | 降噪前AUC | 降噪后AUC |
|--------|-------------|-------------|
| 业务数据集 | 0.763 | **0.894** |



# 弹性算力资源临时调配最佳实践

## 场景
声纹识别批处理任务面临严峻的交付时限压力，实际待处理样本量较预期翻倍，现有计算集群资源无法满足要求。为确保按期交付，需紧急扩容计算资源至原有规模的2倍。经评估，将8台高性能数据库服务器临时抽调配置为人工智能k8s的计算节点，通过Kubernetes集群动态接入的方式实现算力的快速横向扩展。

## 关键操作
1. **资源交付与配置(day1)**
   - 交付保障团队确认8台机器并入集群，配置系统及网络（与原集群对齐）。
2. **应用适配与扩容(day2)**
   - 人工智能团队检查应用内并行度设置，避免计算翻倍导致上游压力激增。
   - 人工智能团队计算最大可扩容副本数（8台机器共112C，单Pod需32C → 最大可新增24Pod）。
   - 人工智能团队执行K8s扩容（原15Pod → 按75%新增算力扩容至33Pod）。
   - 人工智能团队扩容后1h内，监控应用及上下游压力，确保扩容及新增算力的正常运行。
3. **资源回收(15天后)**
   - 人工智能团队在计算完成后，将副本数缩容至计算前的15Pod，确认无任务残留。
   - 交付保障团队禁止8台临时算力机器的调度。
   - 交付保障团队/人工智能团队确认机器无负载后下线。

## 成效
- 全流程耗时2天（从机器准备到计算完成）。
- 无业务中断，生产安全可控。
- 原35天耗时的计算任务，15天完成，提效130%。